# DevodeRator Agents

The active DevodeRator blog-writing agent lives in the shared repo agent bin:

```bash
.agents/devooderator-field-scribe/AGENTS.md
```

This folder is a public-site pointer only. Do not place secrets, private draft notes, passphrases, bearer tokens, signed owner URLs, env values, or handoff material here.
