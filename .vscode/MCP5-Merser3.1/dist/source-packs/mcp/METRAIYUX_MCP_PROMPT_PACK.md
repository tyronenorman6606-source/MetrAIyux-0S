# MetrAIyux 0S · Skye Design MCP Prompt Pack

Use these prompts inside the Skye Design MCP page. Each prompt is written to keep the output copy-paste usable.

## Turn one existing MetrAIyux component into a stronger paste block

```text
Use the Skye Design MCP as a design generation assistant. Context: MetrAIyux 0S is a sovereign operating system interface with deep void backgrounds, blue/cyan/gold signal color, glass panels, proof-first language, and protected owner/client/workspace routing. Task: upgrade the pasted component into one standalone HTML block with scoped CSS and inline SVG only. Keep the visual language premium, futuristic, enterprise, and copy-paste usable. Output one full renderable block. Do not require external images, build tools, CDN scripts, icon fonts, or asset paths. Keep public copy clean and developer-useful.
```

## Generate individual inline SVG icons

```text
Use the Skye Design MCP to generate a set of individual MetrAIyux 0S inline SVG icons. Each icon must be a separate SVG symbol or standalone SVG, not a collage. Use stroke geometry, neon gradient, rounded terminals, and labels that match command, workspace, proof, AI brain, client gate, revenue rail, deployment, database, messaging, and approval concepts. Output copy-ready SVG code with clear names and no external image dependencies.
```

## Expand the true copy-paste vault

```text
Use the Skye Design MCP to create additional true copy-paste MetrAIyux 0S UI blocks. Each block must include scoped CSS, HTML, inline SVG, and a clear title. Build components devs can paste into a blank HTML file and see immediately: hero slab, pricing card, proof rail, command terminal, route selector, workspace dock, status banner, build receipt, token card, and onboarding stepper. No asset paths. No generated-logo replacement. No collage.
```

## Rewrite component copy into proof-safe public language

```text
Use the Skye Design MCP to refine public-facing MetrAIyux 0S component copy. Keep the tone decisive and premium. Avoid exaggerated claims. Distinguish live routes, local components, protected gates, external provider setup, and operator approval where relevant. Return clean UI copy only, plus a compact rationale for which claims were softened or made more precise.
```

## Convert HTML blocks into React components

```text
Use the Skye Design MCP to convert the provided standalone HTML/CSS/SVG component into a React component. Keep the same visual design. Inline SVG should stay local to the component. Use className, not class. Keep styles scoped inside a component-level style tag or provide a CSS string. No external dependencies unless explicitly requested.
```

## Add motion without breaking pasteability

```text
Use the Skye Design MCP to add premium micro-interactions to the provided MetrAIyux 0S component. Use CSS-only animation: glow, float, hover lift, pulse, scanning lines, or reduced-motion fallback. Do not add package dependencies or external scripts. Preserve accessibility, readable contrast, and copy-paste behavior.
```

