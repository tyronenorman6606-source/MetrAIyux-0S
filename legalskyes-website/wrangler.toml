name = "skyes-over-london-legal"
compatibility_date = "2024-09-23"

[site]
bucket = "."

# Cloudflare Pages deployment
# CLI: npx wrangler pages deploy . --project-name=skyes-over-london-legal
# Or drag this folder into Cloudflare Pages dashboard → connect repo → publish directory = root
# Existing _headers and _redirects files are already compatible with CF Pages
