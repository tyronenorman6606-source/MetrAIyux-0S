# Official Source Freshness

Official workflows remain prep-packet routes, not recreated official forms.

Freshness metadata is generated into:

```txt
data/official-workflow-freshness.json
```

API endpoint:

```txt
GET /api/governance/official-freshness
```

The report stores official URL, source ID, last verified date, next review date, and output policy. It does not claim live filing/submission.
