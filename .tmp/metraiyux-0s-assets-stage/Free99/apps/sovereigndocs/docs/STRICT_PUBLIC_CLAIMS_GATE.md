# Strict Public Claims Gate

Run:

```bash
npm run scan:claims
```

The scanner checks public HTML, JavaScript, Markdown, XML, and text files for phrases that could overclaim legal readiness. Negative/warning context is allowed, but affirmative claims like attorney-reviewed, state-compliant, court-ready, official filing, direct submission, guaranteed enforceability, and valid in all states are blocked.
