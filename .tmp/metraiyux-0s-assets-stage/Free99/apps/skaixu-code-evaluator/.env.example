# Optional local fallback. Normal auth is inherited from the 0S/SkyGate session.
METRAIYUX_GATE_SESSION=

# Upstream auth is inherited; this static app does not own sessions.
# Parent shells may inject window.SKAI_UPSTREAM_IDENTITY at runtime.
