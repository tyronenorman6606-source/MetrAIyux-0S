# SkyeRoutexFlow V83 — Bullshit Removed Runtime

Run:

```bash
npm run smoke:v83
npm run check:v83
```

This package keeps the current browser client modules required by `index.html`, but removes old proof/state/directive residue and corrects the backend proof posture.

V83 is not presented as deployed production proof. It is a cleaner, stricter codebase that blocks fake persistence, fake PayPal webhook success, fake deployment receipts, and fake browser-click proof claims.
