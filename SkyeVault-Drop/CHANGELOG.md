# SkyeVault-Drop Changelog

## 2026-06-02

- Shipped the customer-facing Reape0r pass for the paid repo mirror lane. Package `v0.2.4` is now `Reape0r: the Autonomous Cloud Repo Mirror`, defaults to one encrypted mutable current mirror, prints human terminal receipts by default, keeps `--json` for automation, and ships the renamed macOS service template `com.skyevault.reape0r.plist`.
- Reape0r package proof is green against the shipped archive: `20,384` bytes, SHA-256 `9132eb12341546b77a25370fbd667b034f12a459af365aff7d51e006a6a3a372`, platform proof `49/49`, auto-install proof `8/8`, deterministic package rerun stable, and mutable-current verify/restore proof green.
- Reape0r live sellability proof is green after deployment: live HTTP proof `12/12`, sales readiness `18` checks, `6` downloaded-package commands, `156` production stress reads, `0` blockers, and `0` stress failures. Receipts: `test-artifacts/skyevault-agent-live-http/2026-06-02T12-22-26-991Z.json` and `test-artifacts/skyevault-agent-sales-readiness/2026-06-02T12-24-30-893Z.json`.
- Connected the public education lane to the vault and drive surfaces: DevodeRator now has the Reape0r CLI install guide, the `Demon in my Machine` founder note, and the week-of-repairs proof ledger linked from the 0S install center, SkyeVaultPro Drive, account workspace, VaultOS, and command center.
- Closed the owner repo custody lane against the living-current mirror architecture instead of the older baseline-plus-delta owner experience. The production mirror now stores the repo as one current source of truth: normal files as current objects, ignored/secret/private files as encrypted owner-unlock content, and small changed files inside encrypted backend current packs. Owner restore/download reads that current manifest and rebuilds one repaired repo folder; owners do not manually combine a base artifact with separate delta folders.
- Proved the production unlock path against the real MetrAIyux-0S repo export. The encrypted R2 export `MetrAIyux-0S-current-20260601T212752Z.tar.gz.enc` uploaded as `16,341,078,821` bytes with SHA-256 `66c84506007c5d0744a209f96975fb9e3e8aeec50ba199806a2c937a5e911ab0`, then `vault:mirror:unlock` streamed it from R2 through decrypt/gunzip/tar into a repaired folder with `374,298` files, `138,514` directories, `55` symlinks, and `20,690,721,698` restored bytes. The proof compared `.env`, `.git/HEAD`, and `package.json` without printing secret contents and confirmed the restored folder is a real Git work tree. Receipts: `.skyevault-out/living-mirror/metraiyux-0s-owner/MetrAIyux-0S/exports/production-current-20260601T212752Z/CURRENT_REPO_EXPORT_RECEIPT.json` and `.skyevault-out/living-mirror/metraiyux-0s-owner/MetrAIyux-0S/receipts/living-mirror-unlock-20260602T000343Z.json`.
- Ran the current mirror wake after the unlock proof so the cloud source of truth caught up with the live workspace. The new manifest digest is `327549cd2356644d7ff3b74f83ce01a8e20470a2ee4d60889f5609bc034a6865`, with `374,299` files, `512,868` entries, `20,690,727,654` total bytes, `374,291` protected files, `101,121` changed files uploaded, `6` encrypted current packs, `103` manifest shard objects, `6,484,172,873` uploaded bytes, and `0` failed uploads. Receipt: `.skyevault-out/living-mirror/metraiyux-0s-owner/MetrAIyux-0S/receipts/living-mirror-20260602T005231Z.json`.
- Ran the final Reape0r release mirror wake after all package, blog, valuation, and surface edits. The current digest is `1a0bae0cc9e42a4262886ed069e3111b1eefa03ef82e6cf931382be1bd2cd3b1`, current as of `2026-06-02T12:46:27.018Z`, with `374,485` files, `513,107` entries, `20,734,438,871` total bytes, `49` changed files uploaded, `0` upload failures, `103` entry shard objects refreshed, and a successful Cloudflare R2 current-manifest commit. Receipt: `.skyevault-out/living-mirror/metraiyux-0s-owner/MetrAIyux-0S/receipts/living-mirror-20260602T124627Z.json`.
- Fixed the packed-current stability bug found during final daemon proof. Plain current files stored inside encrypted backend packs now keep their logical `plain-current-object` role while carrying `physicalMode: encrypted-pack`, so a no-change daemon wake does not re-upload the same packed files every cycle. R2 proof receipt `test-artifacts/skyevault-living-mirror/2026-06-02T01-35-49-873Z.json` passes the new `noChangeWakeStable` check with `changedCount:0`, `uploaded:0`, and `packObjects:0` on the third wake.
- Added the owner unlock command surface through `npm run vault:mirror:unlock`, kept `.skyevault-out` excluded from mirror scans so private unlock material is not folded back into the repo bundle, and kept the owner-facing launcher on `CURRENT_REPO_BACKUP.html` with `FULL_17GB_REPO_DOWNLOAD.html` as a legacy redirect only.

## 2026-05-31

- Corrected the owner repo backup architecture to the living-current mirror model. `vault:source:start` now starts the daemon as `--mode=mirror --full-current-index --skip-delta`, `tools/skyevault-living-repo-mirror.mjs` defaults to a full current encrypted per-path mirror instead of an adopted base plus owner-visible deltas, and the old `FULL_17GB_REPO_DOWNLOAD.html` launcher now redirects to `CURRENT_REPO_BACKUP.html`. The proof receipt `test-artifacts/skyevault-living-mirror/2026-06-01T00-17-36-868Z.json` proves seed, changed file, new file, tombstone delete, secret encryption, and restore into one current state.
- Completed owner-reenabled live browser closure for the sellable SkyeVault Agent lane. `npm run vault:agent:browser-proof` now passes 12 headed browser flows across desktop and mobile, creates 4 live unpaid SkyePay checkout sessions without charging a card, proves Stripe checkout handoff, pending install lock, owner/shared-gate download, Drop content, DevodeRator content, 100 screenshots, and 0 failures. Final receipt: `test-artifacts/skyevault-agent-live-browser/2026-05-31T22-32-41-667Z.json`.
- Upgraded the paid Agent installer from a manual-only handoff into a real auto-install lane. Release `v0.2.2` adds `skyevault-agent auto-install`, non-interactive `install.sh` mode, private env-file write at mode `0600`, first encrypted baseline sync, optional user service install, and a $13 SkyePay add-on SKU that unlocks installation help without creating a second vault workspace. Proof receipt: `test-artifacts/skyevault-agent-auto-install/2026-05-31T21-03-07-066Z.json` passes 8/8 checks against the shipped tarball.
- Fixed the install-center buyer UX found by browser proof: locked unpaid download clicks now stay on the install page with a clear lock message while the direct package route still returns 402, the mobile header/Owner Login nav stays clickable, and the install center no longer loads the gated root `script.js` for unpaid buyers.
- Re-ran `npm run vault:agent:sales:proof` after browser closure and final valuation-source deploy. Current receipt `test-artifacts/skyevault-agent-sales-readiness/2026-05-31T22-44-15-311Z.json` passes 18 checks, 6 shipped-package commands, and 156 concurrent live stress reads with 0 failures.
- Hardened the sellable SkyeVault Agent lane: FS27 exposes a public buyer page, SkyePay owns Starter/Pro/Command offers, the 0S install center carries SkyePay return sessions into package download links, and package downloads stay locked until owner/shared-gate access or paid workspace provisioning is confirmed.
- The packaged customer agent is now a reproducible `v0.2.2` tarball with SHA-256 `5cb4ceb88b12b5af06684deed401179dcca6c7cbdd3101a3ef2c38223a7518aa` and `15,967` bytes. It ships the Node CLI for `doctor`, `init`, `auto-install`, `snapshot`, `sync`, `watch`, `verify`, and `restore`, plus `install.sh`, `RUNBOOK.md`, and Linux/macOS service templates.
- Extended the platform proof to run the paid SkyePay vault webhook provisioning test and release-alignment checks. The current proof receipt passes 11 commands and 49 checks, including version/manifest/archive alignment, paid checkout provisioning, encrypted full baseline, changed-file delta, tombstones, restore, and buyer portal-key upload lifecycle.
- Added the sales-readiness proof `npm run vault:agent:sales:proof` so the lane is no longer verified by shallow HTTP `200` checks. The current receipt passes 18 sellability checks, 6 shipped-package commands, and 156 concurrent live stress reads with zero failures across the buyer page, buyer alias, SkyePay store shell, store JS checkout hydrator, SkyePay offers API, live unpaid checkout creation, pending-session status, pending package lock, unauthenticated package lock, owner/shared-gate manifest and package download, live downloaded CLI extraction, baseline/delta/verify/restore round trip, valuation source, and SkyeVault Drop surface.
- Redeployed the main 0S Worker/assets through the v0.2.2 auto-install, package, install-center, and valuation refresh; closeout deploy receipt is `610734ef-f39d-4553-a739-1c9c5590abda` at `test-artifacts/0s-worker-deploy/2026-05-31T22-40-47-049Z-worker-deploy.json`. SkyeVault-Drop was redeployed after the final changelog refresh.

## 2026-05-29

- Corrected the repo rescue daemon contract: autosync `full` mode now defaults to encrypted literal all-bytes repo custody, not the smaller filtered source-custody package. Source-custody remains available only as an explicit opt-in for smaller packages.
- Updated the public Agent Install page to tell new operators and coding AIs to run the literal full lane, keep signed links private, and use the owner-gated SkyeVault Drive, SkyeVault Command Center, and SkyeSecure unlocker for recovery.
- Added the owner signed-link finalizer helper `tools/skyevault-mint-receipt-downloads.mjs`. It logs in through the shared FS27/SkyGate/Free99 lane, mints artifact/control-pack download links from a handoff receipt, and writes them to private local receipts without printing bearer tokens.
- Completed the literal owner export after replacing the slower first stream with the concurrent encrypted stream uploader. Final artifact `MetrAIyux-0S-full-repo-20260529T130848Z.tar.zst.enc` uploaded through direct Cloudflare R2 multipart as `15,660,995,840` bytes (`14.59` GiB), artifact receipt `cdv_f4973647019072d97eb62f11`, SHA-256 `a02e131bef14a87d965a6e8cfb201dfab0130a37e7f0613f4373ce08658c85ca`, and SkyeSecure control-pack receipt `cdv_654dcc3550c042e62d041617`. The fast delta journal completed first as receipt `cdv_336a7a9682ddb7e1bb79e22a`, and owner-only signed links were written to `.skyevault-out/autosync/latest-full-repo-download-links.json`.
- Deployed SkyeVault-Drop to Cloudflare Workers as version `b1c2881b-b3db-4a2f-a640-71520ab0be11` after `npm --prefix SkyeVault-Drop run cloudflare:check` passed.

## 2026-05-24

- Added the encrypted SkyeVault delta journal fast lane. Autosync now packs changed/untracked/local-critical files and deletion tombstones into a `.skyesecrets` delta journal before the heavier full snapshot lane, with `npm run vault:delta:dry-run`, `npm run vault:delta:upload`, and `npm run vault:delta:status` as standalone operator commands.
- Updated autosync proof and Resend notifications to expose public-safe delta journal counts, digest, pack size/hash, and receipt IDs while keeping file bodies, passphrases, peppers, signed URLs, and private handoff material out of public surfaces.
- Shipped the SkyeVault autosync parity closeout for the owner repo continuity lane: the local daemon remains the ten-minute scanner, normal repo custody still runs first, and companion SkyeVault Bin exports now run afterward for agent/site lanes without double-writing shared files.
- Added the DevodeRator Field Scribe agent lane. The agent can collect the day's changed paths, recent receipts, MCP proof, deploy evidence, and vault state, then draft a founder/dev blog from that proof bundle with `npm run devooderator:agent:brief` and `npm run devooderator:agent:draft`.
- Added SkyeVault Bin export commands for scoped custody: `npm run vault:bins:export:dry-run`, `npm run vault:bins:export`, and `npm run vault:agents:export`. Uploading companion bin packs remains explicit through `--upload`, `--upload-bins`, `SKYEVAULT_BIN_UPLOAD=1`, or `SKYEVAULT_AUTOSYNC_BIN_UPLOAD=1`.
- Updated the public AI/dev install walkthrough at `https://skyevault-drop.graylondonskyes.workers.dev/agent-install.html` so a new developer or coding AI can start the local agent, understand the no-perfect-tech recovery boundary, run the proof commands, and keep agent exports separate from the main encrypted repo custody lane.
- Deployed SkyeVault-Drop to Cloudflare Workers as version `71bb1cf4-3802-4b54-bc7c-abc3425cff34`.
- Deployed DevodeRator to Cloudflare Pages at `https://devooderator.pages.dev/`; the latest direct-upload ID is recorded in `test-artifacts/cloudflare-pages/devooderator-direct-upload-receipt.json`, the cards route now uses the floating Gray cutout logo, and the bins route documents the agent/bin export workflow.
- Deployed the 0S marketing Pages surface at `https://metraiyux-0s-marketing.pages.dev/` with closeout direct upload `8e008ed4-d8cf-488c-955a-ce02418597c1`, and deployed the Legal Skyes Pages surface at `https://skyes-over-london-legal.pages.dev/` with closeout direct upload `95e0cd7b-d5e4-4482-bf17-2765dbf8b1e5`.
- Redeployed the main 0S Worker at `https://metraiyux-0s-full-system.graylondonskyes.workers.dev` after regenerating the public proof/changelog module; the exact final Worker version is recorded by Wrangler in the closeout output.
- Verified with `npm --prefix SkyeVault-Drop run check`, `npm run devooderator:agent:brief`, `npm run devooderator:agent:draft`, `npm run vault:bins:export:dry-run`, `npm run vault:bins:export`, `npm run vault:autosync:dry-run -- --force --mode=full --skip-map --upload-bins`, and live HTTP smokes for the DevodeRator cards/bins routes plus the SkyeVault-Drop agent install page.
- No vault wipe was performed. The existing vault remains intact as requested.
- Headed browser proof was intentionally stopped and waived by the owner for this closeout; owner live verification is the production signoff path for this entry.

## 2026-05-23

- Added a customer-safe encrypted full-repo restore flow: full workspace artifacts remain protected as `.zip.enc`, and each run can produce a direct restore kit ZIP that unlocks the artifact into the real repo ZIP.
- Added `tools/skyevault-restore-encrypted-zip.mjs`, a standalone helper that extracts the restore kit, decrypts the encrypted repo artifact, verifies the resulting ZIP, and extracts the workspace.
- Updated `tools/skyevault-full-repo-push.mjs` so ZIP full-repo pushes generate a direct restore kit containing `README.txt`, `RESTORE.md`, the artifact key material, and the restore helper. The script also attempts to upload the restore kit unless `--skip-direct-restore-kit-upload` is passed.
- Updated the customer-facing Repo Vault and My Vault pages to explain that `.zip.enc` is encrypted and that the matching direct restore kit is required before unzipping.
- Added `docs/ENCRYPTED_REPO_ZIP_RESTORE.md` with customer/operator restore commands and wording rules.
