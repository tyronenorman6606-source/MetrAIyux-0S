export default `<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Free99 Admin Code Unlock | MetrAIyux 0S</title>
  <meta name="description" content="Free99 admin-code unlock for every MetrAIyux 0S mounted app through the shared FS27 gate.">
  <style>
    :root{color-scheme:dark;--bg:#07090b;--panel:#111817;--line:rgba(255,255,255,.14);--text:#f8fbf6;--muted:#aeb8b2;--gold:#f5d36a;--cyan:#7ae7ff;--danger:#ff6b81}
    *{box-sizing:border-box}
    body{margin:0;min-height:100svh;background:radial-gradient(circle at 10% 0%,rgba(122,231,255,.12),transparent 28%),linear-gradient(145deg,#060708,#0d1312 48%,#100c08);color:var(--text);font-family:Inter,ui-sans-serif,system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;line-height:1.55}
    main{width:min(1040px,calc(100% - 32px));margin:0 auto;padding:56px 0 80px}
    .shell{display:grid;grid-template-columns:minmax(0,1fr) minmax(320px,.82fr);gap:20px;align-items:start}
    .panel{border:1px solid var(--line);background:rgba(17,24,23,.82);border-radius:8px;padding:24px;box-shadow:0 24px 80px rgba(0,0,0,.32)}
    .eyebrow{margin:0 0 10px;color:var(--gold);font-size:12px;font-weight:900;letter-spacing:.14em;text-transform:uppercase}
    h1{margin:0 0 16px;font-size:clamp(42px,7vw,76px);line-height:.96;letter-spacing:0}
    h2{margin:0 0 10px;font-size:24px}
    p{color:var(--muted);margin:0 0 16px}
    label{display:grid;gap:8px;margin:0 0 14px;color:#dce6df;font-weight:900}
    input{width:100%;border:1px solid var(--line);border-radius:8px;background:#07100f;color:var(--text);padding:14px 14px;font:inherit}
    button,a.button{appearance:none;border:1px solid var(--line);border-radius:8px;background:#16201f;color:var(--text);padding:12px 14px;font:inherit;font-weight:900;text-decoration:none;cursor:pointer}
    button.primary{width:100%;border:0;background:linear-gradient(135deg,var(--gold),var(--cyan));color:#06100d}
    .actions{display:flex;flex-wrap:wrap;gap:10px;margin-top:14px}
    .status{min-height:48px;border:1px solid var(--line);border-radius:8px;background:rgba(255,255,255,.05);padding:12px 14px;color:var(--muted);margin-top:14px}
    .status.ok{border-color:rgba(122,231,255,.42);color:#dff9ff}
    .status.bad{border-color:rgba(255,107,129,.48);color:#ffdce3}
    .token-panel{display:none;margin-top:14px}
    .token-panel.active{display:block}
    .token-preview{display:block;word-break:break-all;border:1px solid var(--line);border-radius:8px;background:#050807;padding:12px;color:#dff9ff}
    .list{display:grid;gap:10px;margin-top:16px}
    .list a{color:#dff9ff}
    .fine{font-size:13px;color:#8f9b94}
    .return-box{border:1px solid rgba(122,231,255,.22);border-radius:8px;background:rgba(122,231,255,.06);padding:12px;margin:12px 0 16px;color:#dff9ff;word-break:break-word}
    @media(max-width:820px){main{padding-top:28px}.shell{grid-template-columns:1fr}h1{font-size:44px}}
  </style>
</head>
<body>
  <main>
    <section class="shell">
      <div class="panel">
        <p class="eyebrow">Free99 Admin Gate</p>
        <h1>Enter your Free99 admin code.</h1>
        <p>This is the one shared 0S unlock for every mounted app. No app email. No app password. No second login lane.</p>
        <p class="fine">By continuing through the 0S gate, users should review the Legal Skyes <a href="https://skyes-over-london-legal.pages.dev/legal/twilio-sms/" target="_blank" rel="noopener">Twilio SMS Consent and 0S Data Notice</a>, <a href="https://skyes-over-london-legal.pages.dev/legal/terms/" target="_blank" rel="noopener">Terms</a>, and <a href="https://skyes-over-london-legal.pages.dev/legal/privacy/" target="_blank" rel="noopener">Privacy Policy</a>.</p>
        <form id="owner-login-form" autocomplete="on">
          <label>Free99 Admin Code
            <input name="code" type="password" autocomplete="current-password" required placeholder="Free99 admin code">
          </label>
          <button class="primary" type="submit" id="unlock-button">Unlock This App</button>
        </form>
        <div id="status" class="status" role="status">Waiting for Free99 admin code.</div>
        <div id="token-panel" class="token-panel">
          <p class="eyebrow">Advanced owner bearer</p>
          <code id="token-preview" class="token-preview"></code>
          <div class="actions">
            <button id="copy-token" type="button">Copy Bearer</button>
            <button id="copy-export" type="button">Copy Export Command</button>
            <button id="test-session" type="button">Test Introspection</button>
          </div>
          <p class="fine">This is only for operator API/MCP testing. App access uses the same Free99 admin-code session automatically.</p>
        </div>
      </div>
      <aside class="panel">
        <p class="eyebrow">Requested app</p>
        <h2 id="return-heading">Unlock once, then the app opens.</h2>
        <div id="return-path" class="return-box">The gate will return you to the app that sent you here.</div>
        <p class="fine">Every mounted 0S app must come through this same FS27/Free99 gate. If an app asks for its own email/password, that app is wrong.</p>
	        <div class="list">
	          <a id="direct-return-link" href="/Free99/index.html">Open requested app</a>
	          <a href="/live/company-knowledge-layer-proof.html">Company Knowledge public proof</a>
	          <a href="/Free99/index.html">Free99 App Hub</a>
	          <a href="https://skyes-over-london-legal.pages.dev/legal/twilio-sms/" target="_blank" rel="noopener">Legal Skyes Twilio/0S Notice</a>
	          <a href="/admin/automation-brain.html">0S Admin Automation Brain</a>
          <a href="/api/0s/route-manifest">0S Route Manifest</a>
        </div>
      </aside>
    </section>
  </main>
  <script>
    const form = document.getElementById('owner-login-form');
    const statusBox = document.getElementById('status');
    const tokenPanel = document.getElementById('token-panel');
    const tokenPreview = document.getElementById('token-preview');
    const returnPath = document.getElementById('return-path');
    const returnLink = document.getElementById('direct-return-link');
    const unlockButton = document.getElementById('unlock-button');
    let ownerToken = '';
    const returnParams = new URLSearchParams(location.search);
    const pendingReturnKey = 'FREE99_PENDING_APP_RETURN';
    const returnState = resolveReturnPath();
    const returnTo = returnState.path;

    if (returnTo) {
      returnPath.textContent = returnTo;
      returnLink.href = returnTo;
      returnLink.textContent = 'Open requested app';
      unlockButton.textContent = 'Unlock This App';
    } else {
      returnPath.textContent = 'No app return was supplied. Free99 can unlock the shared 0S session, but no app told the gate where to return.';
      returnLink.href = '/founder-command/?view=core#pocket-skyemail';
      returnLink.textContent = 'Open Pocket SkyeMail';
      unlockButton.textContent = 'Unlock 0S';
    }

    function setStatus(message, kind) {
      statusBox.textContent = message;
      statusBox.className = 'status' + (kind ? ' ' + kind : '');
    }

    function maskToken(token) {
      if (!token) return '';
      return token.slice(0, 18) + '...' + token.slice(-12);
    }

    function safeReturnPath(value) {
      const text = String(value || '').trim();
      if (!text || !text.startsWith('/') || text.startsWith('//')) return '';
      if (/^\\/api\\/owner\\/admin-login/i.test(text) || /^\\/admin\\/login(?:\\/|\\.html)?/i.test(text)) return '';
      return text;
    }

    function pendingReturnPath() {
      try {
        return sessionStorage.getItem(pendingReturnKey) || '';
      } catch (error) {
        return '';
      }
    }

    function referrerReturnPath() {
      try {
        const referrer = new URL(document.referrer || '');
        if (referrer.origin !== location.origin) return '';
        return referrer.pathname + referrer.search + referrer.hash;
      } catch (error) {
        return '';
      }
    }

    function resolveReturnPath() {
      const candidates = [
        returnParams.get('return'),
        returnParams.get('return_to'),
        returnParams.get('returnTo'),
        returnParams.get('next'),
        pendingReturnPath(),
        referrerReturnPath()
      ];
      for (const candidate of candidates) {
        const safe = safeReturnPath(candidate || '');
        if (safe) return { path: safe };
      }
      return { path: '' };
    }

    function persistSharedGateToken(token, source) {
      const clean = String(token || '').replace(/^Bearer(?:\\s+|$)/i, '').trim();
      if (!clean) return;
      const shared = {
        token: clean,
        source: source || 'free99-admin-code',
        platform_id: 'metraiyux-0s',
        usage_lane: 'fs27-owner-gate',
        issued_at: new Date().toISOString()
      };
      localStorage.setItem('quantumskyes_mcp_owner_token', clean);
      sessionStorage.setItem('FREE99_PLATFORM_GATE_SESSION', JSON.stringify(shared));
      localStorage.setItem('FREE99_PLATFORM_GATE_SESSION', JSON.stringify(shared));
      window.MetrAIyuxGateBridge?.persist?.(shared, {silent:true});
    }

    async function copyText(text, label) {
      await navigator.clipboard.writeText(text);
      setStatus(label + ' copied.', 'ok');
    }

    form.addEventListener('submit', async (event) => {
      event.preventDefault();
      const payload = Object.fromEntries(new FormData(form).entries());
      setStatus('Checking Free99 admin code...', '');
      tokenPanel.classList.remove('active');
      ownerToken = '';
      try {
        const response = await fetch('/api/owner/admin-login', {
          method: 'POST',
          credentials: 'include',
          headers: {'content-type': 'application/json'},
          body: JSON.stringify(payload)
        });
        const data = await response.json().catch(() => ({}));
        if (!response.ok) throw new Error(data.error || 'Free99 admin code unlock failed.');
        ownerToken = data.token || '';
        persistSharedGateToken(ownerToken, 'free99-admin-code');
        const gateToken = data.gateToken || data.gateBearerToken || '';
        if (gateToken) {
          const shared = {token: gateToken, source: 'fs27-admin-login', platform_id: 'metraiyux-0s', usage_lane: 'fs27-owner-gate', issued_at: new Date().toISOString()};
          sessionStorage.setItem('adminBrainToken', gateToken);
          sessionStorage.setItem('FREE99_PLATFORM_GATE_SESSION', JSON.stringify(shared));
          localStorage.setItem('FREE99_PLATFORM_GATE_SESSION', JSON.stringify(shared));
          localStorage.setItem('quantumskyes_mcp_owner_token', gateToken);
          window.MetrAIyuxGateBridge?.persist?.(shared, {silent: true});
        }
        tokenPreview.textContent = maskToken(ownerToken);
        if (returnTo) {
          setStatus('Free99 admin code accepted. Opening this app...', 'ok');
          try { sessionStorage.removeItem(pendingReturnKey); } catch (error) {}
          setTimeout(() => location.assign(returnTo), 250);
        } else {
          tokenPanel.classList.add('active');
          setStatus('Free99 admin code accepted. 0S app gate is unlocked.', 'ok');
        }
      } catch (error) {
        setStatus(error.message || 'Free99 admin code unlock failed.', 'bad');
      }
    });

    document.getElementById('copy-token').addEventListener('click', () => {
      if (ownerToken) copyText(ownerToken, 'Bearer token').catch(() => setStatus('Clipboard blocked by browser.', 'bad'));
    });

    document.getElementById('copy-export').addEventListener('click', () => {
      if (ownerToken) copyText('export QUANTUMSKYES_MCP_TOKEN=\\'' + ownerToken + '\\'', 'Export command').catch(() => setStatus('Clipboard blocked by browser.', 'bad'));
    });

    document.getElementById('test-session').addEventListener('click', async () => {
      if (!ownerToken) return;
      setStatus('Testing shared gate session...', '');
      try {
        const response = await fetch('/api/skygate/auth-introspect', {
          method: 'POST',
          headers: {'content-type': 'application/json'},
          body: JSON.stringify({token: ownerToken})
        });
        const data = await response.json().catch(() => ({}));
        if (!response.ok || !data.active) throw new Error(data.error || 'Introspection failed.');
        setStatus('Active Free99 admin session: ' + (data.email || data.role || 'owner') + '.', 'ok');
      } catch (error) {
        setStatus(error.message || 'Introspection failed.', 'bad');
      }
    });
  </script>
</body>
</html>
`;
