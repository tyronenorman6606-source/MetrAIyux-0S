(()=>{window.addEventListener('load',()=>setTimeout(()=>document.body.classList.remove('locked'),5000));const nav=document.getElementById('nav');const onScroll=()=>nav&&nav.classList.toggle('scrolled',scrollY>40);addEventListener('scroll',onScroll,{passive:true});onScroll();const io=new IntersectionObserver(es=>es.forEach(e=>{if(!e.isIntersecting)return;e.target.classList.add('visible');io.unobserve(e.target)}),{threshold:.15});document.querySelectorAll('.fade-up,.stat,.list li').forEach(el=>io.observe(el));const sideLinks=[...document.querySelectorAll('.side a[href^="#"]')];const secs=sideLinks.map(a=>document.querySelector(a.getAttribute('href'))).filter(Boolean);const active=()=>{const y=scrollY+140;let cur=secs[0];for(const s of secs){if(s.offsetTop<=y)cur=s}sideLinks.forEach(a=>a.classList.toggle('active',cur&&a.getAttribute('href')==='#'+cur.id))};addEventListener('scroll',active,{passive:true});active();document.querySelectorAll('.num[data-count]').forEach(el=>{const target=+el.dataset.count;const ob=new IntersectionObserver(es=>es.forEach(e=>{if(!e.isIntersecting)return;const st=performance.now();const step=t=>{const p=Math.min(1,(t-st)/1200);el.textContent=Math.round(target*(1-Math.pow(1-p,3))).toLocaleString();if(p<1)requestAnimationFrame(step)};requestAnimationFrame(step);ob.disconnect()}),{threshold:.3});ob.observe(el)});const c=document.getElementById('stars');if(c){const x=c.getContext('2d',{alpha:true}),d=Math.min(2,devicePixelRatio||1);let W=0,H=0,stars=[];function resize(){W=innerWidth;H=innerHeight;c.width=W*d;c.height=H*d;c.style.width=W+'px';c.style.height=H+'px';x.setTransform(d,0,0,d,0,0);stars=Array.from({length:120},()=>({x:Math.random()*W,y:Math.random()*H,r:.6+Math.random()*1.4,a:.12+Math.random()*.6,v:.08+Math.random()*.2}))}function tick(){x.clearRect(0,0,W,H);for(const s of stars){s.a+=Math.sin(performance.now()/1000*s.v)*.003;x.beginPath();x.fillStyle=`rgba(255,255,255,${Math.max(.08,Math.min(.9,s.a))})`;x.arc(s.x,s.y,s.r,0,Math.PI*2);x.fill()}requestAnimationFrame(tick)}addEventListener('resize',resize,{passive:true});resize();requestAnimationFrame(tick)}})();