import test from 'node:test';
import assert from 'node:assert/strict';
import { customerSegmentRecord, normalizeCustomerSegmentInput, normalizePriceListInput, priceListRecord, resolvePriceListPrice } from '../src/lib/price-lists.js';
test('segments and price lists normalize B2B pricing rules', () => { const segment = normalizeCustomerSegmentInput({ name: 'Wholesale Contractors', rules: { emailDomain: 'builder.com' } }); assert.equal(segment.key, 'wholesale_contractors'); const record = customerSegmentRecord({ id: 'seg_1', merchant_id: 'm1', name: segment.name, segment_key: segment.key, rules_json: JSON.stringify(segment.rules), active: 1 }); assert.equal(record.rules.emailDomain, 'builder.com'); const list = normalizePriceListInput({ name: 'Contractor Net 30', segmentId: 'seg_1', items: [{ productId: 'p1', adjustmentType: 'percent', adjustmentBps: 2000 }] }); assert.equal(priceListRecord({ id: 'prl_1', merchant_id: 'm1', name: list.name, segment_id: 'seg_1', active: 1 }, list.items).items[0].adjustmentBps, 2000); });
test('price list resolver supports percent fixed and override pricing', () => { assert.equal(resolvePriceListPrice(10000, { adjustmentType: 'percent', adjustmentBps: 2500 }).priceCents, 7500); assert.equal(resolvePriceListPrice(10000, { adjustmentType: 'fixed', priceCents: 1500 }).priceCents, 8500); assert.equal(resolvePriceListPrice(10000, { adjustmentType: 'override', priceCents: 6400 }).priceCents, 6400); });
