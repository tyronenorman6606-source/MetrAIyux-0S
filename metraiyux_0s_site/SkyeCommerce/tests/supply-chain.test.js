import test from 'node:test';
import assert from 'node:assert/strict';
import { buildPurchaseOrderReceipt, calculatePurchaseOrderTotals, normalizePurchaseOrderInput, normalizeSupplierInput, purchaseOrderRecord, summarizeReceiving } from '../src/lib/supply-chain.js';
test('supplier and purchase order inputs normalize replenishment data', () => { const supplier = normalizeSupplierInput({ name: 'Phoenix Supply', email: 'BUY@SUPPLY.COM', leadTimeDays: '7', minimumOrderCents: '50000' }); assert.equal(supplier.email, 'buy@supply.com'); const po = normalizePurchaseOrderInput({ supplierId: 'sup_1', items: [{ productId: 'p1', quantity: 4, unitCostCents: 900 }] }); assert.equal(po.totalCostCents, 3600); assert.equal(calculatePurchaseOrderTotals(po.items).unitCount, 4); });
test('purchase order receiving caps at remaining open units', () => { const po = purchaseOrderRecord({ id: 'po_1', merchant_id: 'm1', supplier_id: 'sup_1', status: 'ordered' }, [{ id: 'poi_1', purchase_order_id: 'po_1', product_id: 'p1', quantity: 5, received_quantity: 2, unit_cost_cents: 1000 }]); const receipt = buildPurchaseOrderReceipt(po, { items: [{ itemId: 'poi_1', quantity: 99 }], note: 'dock receipt' }); assert.equal(receipt.items[0].receiveQuantity, 3); assert.equal(receipt.complete, true); assert.equal(summarizeReceiving(receipt.items).receivedUnits, 5); });
