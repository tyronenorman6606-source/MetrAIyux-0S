# Public Claims Pack

## Safe headline

CitadelDB Ultimate is sovereign database command infrastructure by Skyes Over London for proof-backed app database operations, backups, restores, migrations, private routing, and recovery discipline.

## Safe subheadline

Part of the SoveReign13 Infrastructure Family under SOLEnterprises International Nexus & Holdings.

## Safe claims

✅ Company-owned architecture exists  
✅ CitadelDB control-plane foundation exists  
✅ Reliquary backup/restore receipt model exists  
✅ SceptR job/proof runner model exists  
✅ Veyra3.1 private routing doctrine exists  
✅ Aegis policy/credential discipline exists  
✅ SkyLedger audit/claims/proof model exists  
✅ Dashboard source exists  
✅ CLI proof commands exist  
✅ Live proof still required for production claims  

## Claims requiring live receipts

☐ live VPS production deployment  
☐ live app migration from hosted database provider  
☐ live object-storage backup sync  
☐ live point-in-time restore  
☐ live high-availability failover  
☐ live browser proof against deployed dashboard  

## Forbidden phrasing

Do not say:

- fully live enterprise database cloud
- proven high availability
- proven point-in-time recovery
- production deployed for real clients

unless the matching proof receipt exists.

## v1.1 Website Claim

Safe:

```text
CitadelDB Ultimate includes a branded public website and private operator dashboard path.
```

Still receipt-gated:

```text
The private operator dashboard is live production deployed.
```

## v1.2 Dashboard and AI Claims

Safe:

```text
CitadelDB includes a dashboard workflow for creating app databases and viewing app connection instructions.
```

Safe:

```text
CitadelDB includes an optional server-side AI Debug Assistant that can call OpenAI or Gemini when API keys are configured.
```

Not safe without proof:

```text
AI fixed the database.
```

AI guidance is not a proof receipt.

## v1.3 Launchpad Claims

Safe:

```text
CitadelDB includes a Database Launchpad dashboard flow for creating app databases, rotating app credentials, testing connection strings, and running write-smoke checks.
```

Still receipt-gated:

```text
A specific app has been migrated and proven on CitadelDB.
```

## v1.4 Guided Ops Claims

Safe:

```text
CitadelDB includes guided dashboard operations for first-run setup, proof actions, diagnostic bundles, and AI-assisted debugging.
```

Still receipt-gated:

```text
A deployment is proven healthy.
```

## v1.5 Setup Wizard Claims

Safe:

```text
CitadelDB includes a guided setup wizard for required secrets, setup planning, and the operator path from first run to first app database.
```

Still receipt-gated:

```text
The server is secure and production-ready.
```

## v1.6 App Onboarding Claims

Safe:

```text
CitadelDB includes app onboarding packets, framework database templates, and proof packets for app migration acceptance.
```

Still receipt-gated:

```text
A specific app is fully migrated and accepted.
```

## v1.7 App Lifecycle Claims

Safe:

```text
CitadelDB includes app lifecycle workflows for migration planning, proof actions, rollback packets, and acceptance tracking.
```

Still receipt-gated:

```text
A real app has been migrated end-to-end.
```

## v1.8 Self-Service Console Claims

Safe:

```text
CitadelDB includes a self-service console for projects, database provisioning, connection strings, SQL execution, and query history.
```

Still receipt-gated:

```text
CitadelDB is ahead of incumbent hosted database platforms.
```

## v1.9 Platform Closure Claims

Safe:

```text
CitadelDB includes platform foundations for accounts, teams, plans, quotas, usage scaffolding, self-service projects, SQL console, and table browsing.
```

Still receipt-gated:

```text
CitadelDB has full live billing, autoscaling, branching, and hosted-database providers parity.
```

## v2.0 Commercial Control Plane Claims

Safe:

```text
CitadelDB includes commercial control-plane foundations for subscriptions, entitlements, Stripe-compatible webhook handling, branch requests, and commercial readiness checks.
```

Still receipt-gated:

```text
CitadelDB has live Stripe billing, enforced subscriptions, live branching, and complete hosted-platform parity.
```

## v2.1 Live Gate Hardening Claims

Safe:

```text
CitadelDB includes live-gate hardening for entitlement checks, paid route guard hooks, usage event recording, branch proof packets, and commercial gate visibility.
```

Still receipt-gated:

```text
All paid routes are fully enforced and live subscription blocking is proven.
```

## v2.2 Truth Correction Claims

Safe:

```text
CitadelDB includes an overclaim audit ledger, protected route registry, expanded paid-route guard hooks, raw webhook body capture, branch clone worker script, and hard proof truth scanner.
```

Still receipt-gated:

```text
CitadelDB is live-proven against hosted-database providers or fully production-closed.
```
