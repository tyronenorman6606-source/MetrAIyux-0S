{
  "type": "workflow",
  "name": "Patch Bundle Review Workflow",
  "version": "1.0.0",
  "rules": [
    "Patch bundles must be JSON inside a fenced ```json block.",
    "Each update/create action must include full file contents, not diffs.",
    "Binary files must not be emitted in the patch bundle.",
    "Patch application requires skAI mode to be armed.",
    "After apply, refresh preview and export the project zip or proof pack."
  ]
}
