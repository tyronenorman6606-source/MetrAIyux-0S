# SovereignDocs Local Data

This folder is used by the optional Node API server.

Generated files:

- `vault.json` — local persisted document drafts
- `audit.json` — local API audit log

For real production, replace this adapter with Postgres, D1, R2, S3, or your upstream storage system.
